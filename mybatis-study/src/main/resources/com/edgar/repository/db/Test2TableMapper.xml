<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edgar.repository.Test2TableMapper">
	<resultMap id="Test2TableResultMap" type="Test2Table">
		<result column="test_id" property="testId" />
		<result column="test_code2" property="testCode2" />
		<result column="dict_name" property="dictName" />
		<result column="parent_code" property="parentCode" />
		<result column="sorted" property="sorted" />
	</resultMap>

	<sql id="all_column">
		test_id, test_code2, dict_name, parent_code, sorted
	</sql>

	<sql id="limit">
		<if test="limit != null">
			limit
			<if test="offset != null">
			#{offset},
			</if>
			#{limit}
		</if>
	</sql>

	<insert id="insert" parameterType="Test2Table">
		insert into 
		test2_table(test_id, test_code2, dict_name, sorted) 
		values(#{testId}, #{testCode2}, #{dictName}, #{sorted})
	</insert>

	<delete id="deleteByPrimaryKey" parameterType="map">
		delete from test2_table 
		where test_code2 = #{test_code2} and test_id = #{test_id}
	</delete>

	<update id="updateByPrimaryKey" parameterType="Test2Table">
		update test2_table
		<set>
 			<if test="testId != null"> 
				test_id = #{testId},
			</if>
			<if test="testCode2 != null"> 
				test_code2 = #{testCode2},
			</if>
			<if test="dictName != null"> 
				dict_name = #{dictName},
			</if>
			<if test="sorted != null"> 
				sorted = #{sorted},
			</if>
		</set> 
		where test_code2 =  #{testCode2} 
		and test_id =  #{testId}
	</update>

	<select id="selectByPrimaryKey" resultMap="Test2TableResultMap" parameterType="map">
		select test_id, test_code2, dict_name, parent_code, sorted 
		from test2_table 
		where test_code2 = #{test_code2} 
		and test_id = #{test_id}
	</select>

	<!-- START 写在START和END中间的代码不会被替换 -->
    <select id="count" resultType="int">
        select count(*) from test2_table
    </select>
    <update id="createTable">
        create table `test2_table` (
        `test_id` varchar(32) not null,
        `test_code2` varchar(32) not null,
        `dict_name` varchar(32) not null,
        `parent_code` varchar(32) not null default '-1',
        `sorted` int(11) not null default '9999',
        primary key (`test_code2`,`test_id`)
        ) engine=innodb default charset=utf8;
    </update>
    <update id="dropTable">
        drop table if exists `test2_table`;
    </update>
	<!-- END 写在START和END中间的代码不会被替换-->

</mapper>