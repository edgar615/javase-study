<?xml version="1.0" encoding="UTF-8"?>
<hazelcast
        xsi:schemaLocation="http://www.hazelcast.com/schema/config
        http://www.hazelcast.com/schema/config/hazelcast-config-3.0.xsd "
        xmlns="http://www.hazelcast.com/schema/config"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <!--
        The path defined by the property hazelcast.config
    hazelcast.xml in the classpath if classpath is included in the hazelcast.config
    The working directory
    If all else fails, hazelcast-default.xml is loaded witch is in the hazelcast.jar.
    If one dose not want to deal with a configuration file at all, the configuration can be done programmatically.
    -->
    <network>
        <join>
            <multicast enabled="true"/>
        </join>
    </network>

    <map name="a"></map>

    <set name="set">
        <item-listeners>
            <item-listener include-value="true">com.edgar.hazelcast.coll.CollectionItemListener</item-listener>
        </item-listeners>
    </set>

    <list name="list">
        <item-listeners>
            <item-listener include-value="true">com.edgar.hazelcast.coll.CollectionItemListener</item-listener>
        </item-listeners>
    </list>

    <queue name="queue">
        <max-size>10</max-size>
        <item-listeners>
            <item-listener include-value="true">com.edgar.hazelcast.coll.CollectionItemListener</item-listener>
        </item-listeners>
        <queue-store>
            <class-name>com.edgar.hazelcast.coll.QueueQStore</class-name>
            <properties>
                <property name="binary">false</property>
                <property name="memory-limit">10000</property>
                <property name="bulk-load">500</property>
            </properties>
        </queue-store>
    </queue>
</hazelcast>